# Optional. Get a free key at https://console.groq.com — used only as a
# fallback for tricky item-name matching (slang, typos, transliteration).
# The app works fine without it too; unmatched items just show as-is.
GROQ_API_KEY=your_groq_key_here
